# ============================================================
# Ternak Device Changer — Settings (sett.txt)
# Edit file ini atau toggle lewat WebUI (Settings tab)
# Format: KEY=true  → fitur aktif
#         KEY=false → fitur mati
# ============================================================

# --- Identitas Perangkat ---
ENABLE_PROFILE_SPOOF=true       # Ganti brand/model/fingerprint (apply_profile)
ENABLE_ANDROID_ID=true          # Randomize ANDROID_ID + wipe SSAID
ENABLE_GAID=true                # Randomize Google Advertising ID (GAID)
ENABLE_MAC_RANDOM=true          # Randomize MAC address wlan0
ENABLE_BT_NAME=true             # Randomize nama Bluetooth + restart BT
ENABLE_HOSTNAME=true            # Ubah hostname (net.hostname)

# --- Wipe & Clear ---
ENABLE_CLEAR_APPS=true          # pm clear target apps (Shopee/Tokopedia/TikTok)
ENABLE_WIPE_SDCARD=true         # Hapus data di /sdcard/Android/data/<pkg> (diganti ke /data/media)
ENABLE_WIPE_FIREBASE=true       # Wipe Firebase Installation ID
ENABLE_WIPE_MEDIADRM=true       # Wipe MediaDrm L3 (DRM reset)
ENABLE_RESET_GSF=true           # Reset GSF (Google Services Framework) ID
ENABLE_CLEAR_NETWORK=true       # Wipe network caches + DNS
ENABLE_WIPE_FORENSIC=true       # Hapus logcat/tombstone/dropbox/recent tasks
ENABLE_WIPE_CLIPBOARD=true      # Bersihkan clipboard

# --- Keamanan / Stealth ---
ENABLE_SEAL=true                # Seal Build.* props via resetprop-rs (anti-revert)
ENABLE_POST_FS_DATA=true        # Generate post-fs-data.sh hook (persist antar reboot)
ENABLE_SYSTEM_PROP=true         # Tulis system.prop (load otomatis KSU/Magisk)

# --- Lain-lain ---
ENABLE_BACKUP=true              # Backup state sebelum setiap FRESH
ENABLE_PERSONAS=true            # Simpan persona snapshot per-app
FORCE_APPLY=false               # Paksa apply_profile meski PIF aktif (override konflik)
